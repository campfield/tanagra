%
% In options.tex we stated with did not want an index generated, for other types of content
%   but for this work which is a standard document we do desire an index, so we use renewcommand to set it to actually be created.
%

\renewcommand{\indexgen}{true}
