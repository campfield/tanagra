%
% The subject (subjectid) encapsulates working units managing projects.  In our example this can be a department,
%  school course, or a secret cabal.
%
% import options (variables and new commands) at source level.
\subimport{}{options}
\subimport{\subjectid}{default}
